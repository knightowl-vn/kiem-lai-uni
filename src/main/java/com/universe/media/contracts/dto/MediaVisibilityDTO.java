package com.universe.media.contracts.dto;

/**
 * Public access visibility level of a media asset exposed across module boundaries.
 */
public enum MediaVisibilityDTO {
    PUBLIC,
    PRIVATE,
    RESTRICTED
}
