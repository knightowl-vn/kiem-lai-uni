package com.universe.media.domain;

/**
 * Fundamental category of a media asset.
 *
 * Immutable once assigned to a MediaAsset.
 */
public enum MediaType {
    IMAGE,
    AUDIO,
    VIDEO,
    DOCUMENT
}
