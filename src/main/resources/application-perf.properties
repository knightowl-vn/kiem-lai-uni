# Explicit, development-only latency instrumentation profile.
kiemlai.performance.instrumentation.enabled=true

# Statement text is intentionally not logged; counts and durations come from the JDBC proxy.
spring.jpa.show-sql=false
